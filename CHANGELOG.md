# 更新日志

本文件记录本展示前端面向维护者和使用者的显著变化。

## [Unreleased]

### 新增

- 增加 `detail_rules.py`，为 8–20 共 13 个评分项维护对象、动作、时序和完成状态的详细核验依据。
- 增加只读“展开详细表单”抽屉：未完成项目显示核验维度骨架，进入终态后显示逐条依据、核验状态、证据关联、风险边界和分析链。
- 增加证据缩略图悬停预览与高清灯箱，支持键盘聚焦、触屏点击、背景点击和 Esc 关闭。
- 增加详情结果的实时回填、校验、mock 事件和重置清空逻辑；评分仍由实时绑定状态归一化。

### 调整

- 详细核验框架统一按可见对象、动作过程、时间顺序和完成状态组织，页面只展示当前项目的实时画面与结果。
- 标准现场和 mock 均以“所有操作按规范完成”为预置基线；结论、评价和逐条依据保存在隐藏的 `prefilled_result`，初始公开投影仍从 `0/13` 和“待开始”开始。
- 完整必需证据写入后由服务端启动 8–20 秒分析窗口，窗口结束才绑定预置依据并显示 `1 / 1 分`；重置只清空实时证据、时间和活动结果，mock 仅保留事件队列。
- mock 生成器按项目最终得分从 10 或 29 个真实视频中随机选取一个正确样本，只使用该样本在对应项目分析过程中生成的关键帧、mask 或 bbox；每个 mock 事件的分析窗口统一为 3 秒，不把历史判断描述复制到观众页面。

## [0.1.0] - 2026-09-02

### 新增

- 增加发动机气缸盖拆装评分项 8–20 的实时证据展示。
- 增加基于 Engine workflow trace 的评分项难度和分析工具链 profile。
- 增加 10 个视频 artifacts 驱动的 mock JSON 与自包含 HTML 回放。
- 增加实时报告 API，支持逐项回填、轮询和重置。

### 调整

- 评分项终态统一显示为“已完成评分”或“待人工确认”。
- 评分难度使用“困难 / 中等 / 简单”，仅在终态展示。
- 分数按实时状态显示：完成评分为 1 分，人工确认计 0 分，分析中暂不显示单项分数。
- 终态展示当前评分项的实际分析任务数量和工具链，不展示跨视频均值或证据类别数量。
- 前端主操作改为“启动评测”，移除“下一项”和“自动播放”交互。

### 修复

- 修复重置后现场绑定、证据、时间和分数未完全清空的问题。
- 修复公开 HTML 泄露内部来源路径、离线运行标识和预置评分文案的问题。
