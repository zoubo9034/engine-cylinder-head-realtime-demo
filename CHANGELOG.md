# 更新日志

本文件记录本展示前端面向维护者和使用者的显著变化。

## [Unreleased]

### 新增

- 增加与图片证据互斥的视频证据模式，标准现场模板和 mock 回放均提供端砚 UI 与世赛 UI 页面；每个评分项可通过 JSON 绑定一个 MP4 地址。
- 增加受控视频媒体接口，支持 GET、HEAD 和单段 Range 请求，并按项目与证据 ID 校验证据归属。
- 增加 13 个由对应正确视频及其项目分析时间生成的视频 Mock；统一编码为 960×540、10fps、H.264 MP4，并移除音轨。
- 同步 `worldskills-redesign-v2/` Coursera 课程页风格 V4 前端，并将其设为后续视觉与交互修改基线；评分规则、报告 JSON 和服务端接口继续复用根目录实现。
- 增加主区顶部 16:9 实时视频接入占位窗口，为后续视频流挂载保留稳定的 `video` 元素入口。
- 增加六阶段“模型工作状态”跑灯，按真实 workflow trace 的阶段分组在标准页和 mock 页同步展示分析过程。
- 增加评分卡渐进展示与最新完成项自动居中聚焦；“待开始”项目在定位前不生成卡片。
- 增加 `detail_rules.py`，为 8–20 共 13 个评分项维护对象、动作、时序和完成状态的详细核验依据。
- 增加只读“展开详细表单”抽屉：未完成项目显示核验维度骨架，进入终态后显示逐条依据、核验状态、证据关联、风险边界和分析链。
- 增加证据缩略图悬停预览与高清灯箱，支持键盘聚焦、触屏点击、背景点击和 Esc 关闭。
- 增加详情结果的实时回填、校验、mock 事件和重置清空逻辑；评分仍由实时绑定状态归一化。

### 调整

- 世赛 video UI 从课程页重构为 1760px 宽的赛事判定台：保留 Logo，移除大 Banner 和五指标卡，改用紧凑状态顶栏、固定 8—20 流程栏、2:3 实时竖屏主区和完整评分卡流；在 1920px 全屏窗口两侧各保留约 80px 余量。图片证据页与端砚 UI 不随本次重构改版。
- 恢复世赛 video UI 上一版的蓝色赛事背景、圆环/暖色光斑装饰和大尺寸联合 Logo，并适当增高顶栏与左侧实操区；评分卡左侧摘要改为全高布局，将分析链贴近卡片底部，减少无意义留白。
- 世赛评分卡证据位保持 9:16，原始 16:9 视频以 `contain` 完整显示，并以同帧 Canvas 实时生成模糊延展背景；Canvas 不可用时回退为深色渐变。
- 重构世赛 UI 评分卡的信息层级：强化项目编号、标题、分数、状态、现场时间、置信度、证据和详情入口的分区，并统一“放大查看”和“已完成 · 查看”入口的图标、文案与点击面积。
- 图片与视频证据统一经过同一状态机、详情引用和公开投影；视频模式下当前选择或最新完成项目自动播放，其他证据播放器自动暂停。
- 视频 Mock 延续 3 秒“证据生成中”窗口；视频片段时长独立取自同项目视觉分析帧范围，避免将不相关的汇总时间标记扩入片段。
- 扩大 V4 主工作区的桌面端纵向高度，使右侧评分区在常见窗口尺寸下可以完整容纳一张完成态项目卡片，同时继续支持卡片列表内部滚动。
- 将主区重排为左侧视频控制区和右侧单列评分卡：跑灯、当前状态窗口及完整工具栏移到视频下方，桌面端左栏整体吸顶，窄屏仅视频吸顶。
- 主区调整为“顶部跑灯 + 左侧实时视频 + 右侧单列评分卡”的左右两栏布局；评分卡在右侧容器内纵向滚动，最新完成项自动居中，手动查看时自动聚焦短暂暂停后恢复。
- 报告 presentation 增加视频占位和工作流展示配置；跑灯使用 3 秒窗口、非均匀阶段权重和受限随机抖动，状态离开“证据生成中”后立即停止，不影响评分与证据绑定。
- 详细核验框架统一按可见对象、动作过程、时间顺序和完成状态组织，页面只展示当前项目的实时画面与结果。
- 标准现场和 mock 均以“所有操作按规范完成”为预置基线；结论、评价和逐条依据保存在隐藏的 `prefilled_result`，初始公开投影仍从 `0/13` 和“待开始”开始。
- 完整必需证据写入后由服务端启动 8–20 秒分析窗口，窗口结束才绑定预置依据并显示 `1 / 1 分`；重置只清空实时证据、时间和活动结果，mock 仅保留事件队列。
- mock 生成器按项目正确性从 10 或 29 个真实视频中随机选取一个样本，只使用该样本在对应项目分析窗口中生成的关键帧、mask 或 bbox；在同一正确性层内均匀抽样，每个 mock 事件的分析窗口统一为 3 秒，不把历史判断描述复制到观众页面。

### 修复

- 修复世赛评分卡随项目数量增加被 Grid 可伸缩行压扁的问题；评分列表改为按内容高度生成独立行，卡片高度不参与剩余空间分配。
- 修复世赛 UI 视频样式块未闭合导致详情抽屉、视频放大灯箱及其后续规则失效的问题；抽屉改为独立视口固定层，灯箱使用受限容器完整显示视频。
- 修复重新聚焦历史视频卡片不续播、自动跳转打断手动浏览的问题；新增“跟随最新 / 手动浏览”状态与“项目 N 已完成 · 查看”提示条，滚动到列表底部自动恢复跟随，并区分系统暂停与用户手动暂停。
- 修复下一评分项定位或分析时提前停止上一卡片视频、卡片重绘导致播放进度归零的问题；现在仅在新完成卡片滚动居中后切换播放，历史卡片再次聚焦时从暂停位置继续。
- 修复视频 Mock 依赖服务当前报告导致的 404 播放失败；固定片段改用受限公开媒体地址，并增加启动前探测、加载失败提示及更稳定的自动/手动播放器切换。
- 修复根目录页面和 V4 通过 HTTP 播放 mock 时同时轮询实时 API，导致已完成项目被服务端初始状态回退为“待开始”并从结果区消失的问题；mock 事件与实时轮询现在互斥，状态合并同时拒绝倒退更新。

## [0.1.0] - 2026-09-02

### 新增

- 增加发动机气缸盖拆装评分项 8–20 的实时证据展示。
- 增加基于 Engine workflow trace 的评分项难度和分析工具链 profile。
- 增加 10 个视频 artifacts 驱动的 mock JSON 与自包含 HTML 回放。
- 增加实时报告 API，支持逐项回填、轮询和重置。

### 调整

- 评分项终态统一显示为“已完成评分”或“待人工确认”。
- 评分难度使用“困难 / 中等 / 简单”，仅在终态展示。
- 分数按实时状态显示：完成评分为 1 分，人工确认计 0 分，分析中暂不显示单项分数。
- 终态展示当前评分项的实际分析任务数量和工具链，不展示跨视频均值或证据类别数量。
- 前端主操作改为“启动评测”，移除“下一项”和“自动播放”交互。

### 修复

- 修复重置后现场绑定、证据、时间和分数未完全清空的问题。
- 修复公开 HTML 泄露内部来源路径、离线运行标识和预置评分文案的问题。
